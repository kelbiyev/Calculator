#include <cmath>
#include <string>
#include <vector>
#include <cctype>
#include <stack>

bool isValidExpression(const std::string& str);
double Calculate(const std::string& str);
double CalcBrackets(std::string str);
